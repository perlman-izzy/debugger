# debugger

Empty scratch repository initialized solely to permit isolated cloud-execution branches.

No validated-agent implementation belongs on `main`; active bootstrap work is confined to dedicated branches.
